# Allocation validation regression

The env implementation accepts a negative cross-project allocation and does not sort lines. The focused test requires rejection and deterministic valid-plan output.
