# cry046__023

Imported entry metadata aliases caller-owned maps, so later caller mutation changes persisted business data.

The supplied focused test deterministically demonstrates the failure in the bug-injected environment and passes in the private corrected oracle.