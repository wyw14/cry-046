# Reconciliation identity regression

The env implementation keys existing entries only by SourceID, so two fingerprints collide. The focused test demonstrates the false conflict and passes against gold.
